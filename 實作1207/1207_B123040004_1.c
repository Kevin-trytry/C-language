#include<stdio.h>
#include<stdlib.h>

void permutation(int x){
	int arr[x]=x;
	printf("All permutation of 1 to %d :\n",x);
	if()
}
int main(){
	int n;
	printf("Enter a positive integer (1 <= n <= 10): ");
	scanf("%d",&n); 
	permutation(n);
}
